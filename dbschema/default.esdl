module default {
	type Contact{
		required firstName: str;
		required lastName: str;
		required email: str;
		description: str;
		required birthDate: str;
		required status: bool;
		required title: str
	}
}
